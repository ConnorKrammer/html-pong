// This fetches that div element that we set the id attribute on.
var div = document.getElementById('content');

// The element is an object, so we can change its properties.
// Here we set the background color to red. This is equivalent to setting the property in CSS.
div.style.backgroundColor = "red";